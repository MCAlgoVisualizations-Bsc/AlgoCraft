package io.github.mcalgovisualizations.algorithms;

import io.github.mcalgovisualizations.algorithms.context.GridContext;
import io.github.mcalgovisualizations.visualization.algorithm.IPlayerSort;

public class PlayerTSTSearch<T extends Comparable<T>> implements IPlayerSort<GridContext<T>> {
    @Override
    public void run(GridContext<T> context) {
        // Disabled for now during graph transition
    }
}
